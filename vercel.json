{
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/nifty-pay run build",
  "outputDirectory": "artifacts/nifty-pay/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite"
}
